{
  "version": 2,
  "buildCommand": "npx vite build --config vite.config.vercel.ts",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}